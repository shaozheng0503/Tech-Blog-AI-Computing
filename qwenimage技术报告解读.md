好的，我将分批次为您完整翻译这份《Qwen-Image Technical Report》。由于整篇技术报告内容非常庞大，我会按照章节逐步翻译，确保准确性与可读性。我们先从**摘要**和**第1章 引言**开始：

---

### **摘要（Abstract）**

我们推出 Qwen-Image——Qwen 系列中的图像生成基础模型，在复杂文本渲染与精准图像编辑方面取得了显著进展。为应对复杂文本渲染的挑战，我们设计了一套全面的数据流水线，涵盖大规模数据收集、过滤、标注、合成与平衡。此外，我们采用渐进式训练策略：从“非文本到文本渲染”起步，逐步从简单文本输入过渡到复杂文本输入，最终扩展至段落级描述。这种课程式学习方法极大提升了模型原生文本渲染能力。因此，Qwen-Image 不仅在英语等字母语言上表现卓越，在更具挑战性的汉语等语素语言上也取得显著进步。

为提升图像编辑一致性，我们提出一种改进的多任务训练范式，不仅包括传统的文本到图像（T2I）和文本-图像到图像（TI2I）任务，还引入图像到图像（I2I）重建任务，从而在 Qwen2.5-VL 与 MMDiT 之间有效对齐潜在表征。此外，我们将原始图像分别输入 Qwen2.5-VL 和 VAE 编码器，分别获得语义表征与重建表征。这种双编码机制使编辑模块在保持语义一致性与视觉保真度之间取得平衡。

我们在多个公开基准上进行了全面评估，包括通用图像生成基准 GenEval、DPG、OneIG-Bench，以及图像编辑基准 GEdit、ImgEdit 和 GSO。Qwen-Image 均达到当前最佳性能，展现出其在图像生成与编辑两方面的强大能力。此外，在 LongText-Bench、ChineseWord 和 CVTG-2K 上的结果表明，Qwen-Image 在文本渲染方面表现尤为突出，尤其在中文文本生成方面，显著优于现有最佳模型。这凸显了 Qwen-Image 作为领先图像生成模型的独特地位——兼具广泛通用能力与卓越文本渲染能力。

---

### **第1章 引言（Introduction）**

图像生成模型——包括文本到图像生成（T2I）与图像编辑（TI2I）——已成为现代人工智能的基础组成部分，使机器能够根据文本提示合成或修改具有视觉吸引力与语义一致性的内容。近年来，该领域取得了显著进展，尤其是扩散架构的出现，使得高分辨率图像生成成为可能，并能捕捉细粒度语义。

尽管取得了这些进展，两大关键挑战依然存在：

1. **文本到图像生成中，模型输出与复杂、多维度提示的对齐仍是重大难题。** 我们的评估表明，即使是当前最先进的商业模型（如 GPT Image 1 与 Seedream 3.0），在面对多行文本渲染、非字母语言（如中文）渲染、局部文本插入或文本与视觉元素无缝融合等任务时，仍表现不佳。

2. **图像编辑中，编辑输出与原始图像的精准对齐面临双重挑战：**
   - 视觉一致性：仅修改目标区域，同时保留其他视觉细节（例如仅改变头发颜色而不影响面部特征）。
   - 语义一致性：在结构性变化中保持全局语义（例如在修改人物姿势的同时保持身份与场景一致）。

在本研究中，我们推出 Qwen-Image，这是 Qwen 系列中一种新型图像生成模型，旨在通过全面的数据工程、渐进式学习策略、增强的多任务训练范式与可扩展基础设施，克服上述挑战。

为解决复杂提示对齐的挑战，我们构建了一个健壮的数据流水线，涵盖大规模收集、标注、过滤、合成增强与类别平衡。我们进一步采用课程式学习策略，从基础文本渲染任务起步，逐步进阶至段落级与布局敏感的描述。这种方法显著提升了模型处理多语言（尤其是汉语等语素语言）的能力。

为解决图像对齐的挑战，我们提出一种增强的多任务学习框架，在共享潜在空间中无缝整合 T2I、I2I 与 TI2I 目标。具体而言，输入图像被编码为两种互补的特征表征：通过 Qwen-VL 提取的语义特征（捕捉高层场景理解与上下文含义），以及通过 VAE 编码器获得的重建特征（保留低层视觉细节）。两组特征共同作为条件信号输入 MMDiT 架构。这种双重条件设计使模型能够同时保持语义一致性与视觉保真度。

为确保大规模训练的效率与稳定性，我们设计了一种基于 TensorPipe 的 Producer-Consumer 框架，用于分布式数据加载与预处理。Producer 负责 VAE 编码与数据 I/O 等预处理任务，而 Consumer 则使用 Megatron 框架专注于分布式模型训练。我们还实施了广泛的监控工具，以确保大规模训练过程中的可靠收敛与调试能力。

Qwen-Image 在根据复杂文本提示生成高质量图像，以及执行准确、上下文感知的图像编辑方面均取得显著进展。该模型能够解释复杂的语言结构，并生成既符合语义意图又满足视觉约束的视觉吸引力输出。为验证其有效性，我们在多种任务上评估了 Qwen-Image，包括文本到图像生成与图像编辑。

Qwen-Image 的主要贡献总结如下：

- **卓越的文本渲染能力**：Qwen-Image 在复杂文本渲染方面表现卓越，包括多行布局、段落级语义与细粒度细节。它对英语等字母语言与汉语等语素语言均支持高保真渲染。
- **一致的图像编辑能力**：通过增强的多任务训练范式，Qwen-Image 在编辑操作中保持语义与视觉真实感方面表现优异。
- **跨基准的强劲表现**：在多个基准上评估表明，Qwen-Image 在各种生成与编辑任务中均优于现有模型，为图像基础模型奠定了坚实基础。


好的，我们继续翻译**第2章 模型架构（Model Architecture）**。由于这一章内容较长，我会分段翻译，先给出**第2章整体概述**和**2.1节 模型架构概览**，然后逐步完成后续小节。

---

### **第2章 模型架构（Model Architecture）**

#### **2.1 模型架构概览（Model Architecture Overview）**

如图6所示，Qwen-Image 架构由三个核心组件协同构成，以实现高保真文本到图像生成：

1. **多模态大语言模型（MLLM）**：作为条件编码器，负责从文本输入中提取特征。
2. **变分自编码器（VAE）**：作为图像分词器（tokenizer），将输入图像压缩为紧凑的潜在表征，并在推理阶段将其解码回图像空间。
3. **多模态扩散Transformer（MMDiT）**：作为主干扩散模型，建模噪声与图像潜在变量在文本引导下的复杂联合分布。

虽然本节概述了它们的一般角色，但具体模型选择与架构细节将在下文详述。

---

#### **2.2 多模态大语言模型（Multimodal Large Language Model）**

Qwen-Image 采用 Qwen2.5-VL 模型（Bai et al., 2025）作为文本输入的特征提取模块，原因有三：

1. **语言与视觉空间已对齐**：相比纯语言模型（如 Qwen3），Qwen2.5-VL 更适合文本到图像任务。
2. **保留语言建模能力**：其语言建模能力未显著退化。
3. **支持多模态输入**：使 Qwen-Image 能解锁更广泛功能（如图像编辑）。

给定用户输入（提示与图像），我们使用 Qwen2.5-VL 提取特征。为更好地指导模型生成精炼的潜在表征，我们针对纯文本输入与文本-图像混合输入分别设计系统提示（如图7与图15所示）。最终，我们使用 Qwen2.5-VL 语言模型主干最后一层的隐藏状态作为用户输入的表征。

---

#### **2.3 变分自编码器（Variational AutoEncoder）**

强大的 VAE 表征对于构建强大的图像基础模型至关重要。当前主流方法通常在大型图像数据集上用 2D 卷积训练图像 VAE，以获得高质量图像表征。相比之下，我们的目标是构建一种更通用的视觉表征，兼容图像与视频。然而，现有联合图像-视频 VAE（如 Wan-2.1-VAE）通常存在性能权衡，导致图像重建能力下降。

为此，我们采用**单编码器-双解码器架构**：
- 共享编码器兼容图像与视频。
- 为每种模态（图像/视频）分别使用专门解码器，使图像基础模型可作为未来视频模型的主干。

具体而言，我们复用 Wan-2.1-VAE 架构，冻结其编码器，仅微调图像解码器。为提升小文本与细粒度细节的重建保真度，我们在内部文本丰富图像语料上训练解码器。该语料包括真实文档（PDF、PPT、海报）与合成段落，覆盖英语与汉语。

训练过程中我们观察到：
1. 用感知损失（perceptual loss）平衡重建损失可有效减少网格伪影（如灌木丛等重复纹理）。
2. 重建质量提升后，对抗损失（adversarial loss）会失效（因判别器无法提供有效指导）。

因此，我们仅使用重建与感知损失，并在微调中动态调整其比例。有趣的是，仅微调解码器即可有效增强细节并改善小文本渲染，为 Qwen-Image 的文本渲染能力奠定坚实基础。

---

#### **2.4 多模态扩散Transformer（Multimodal Diffusion Transformer）**

Qwen-Image 采用多模态扩散Transformer（MMDiT）（Esser et al., 2024）联合建模文本与图像，该方法在 FLUX 系列与 Seedream 等工作中已证明有效。

在每层中，我们引入一种**新颖的位置编码方法：多模态可扩展RoPE（MSRoPE）**。如图8所示，我们比较了多种文本-图像联合位置编码策略：

- **传统 MMDiT**：文本token直接拼接在展平图像位置嵌入后。
- **Seedream 3.0**：将图像位置编码移至图像中心区域，文本token视为2D token（形状[1,L]），使用2D RoPE。
- **MSRoPE（我们的方法）**：将文本输入视为2D张量，所有位置ID在二维上相同。如图8(C)所示，文本被概念化为沿图像对角线拼接。这种设计使 MSRoPE 在图像侧利用分辨率缩放优势，同时在文本侧保持与1D-RoPE的功能等价，无需为文本确定最优位置编码。

---
好的，我们继续翻译**第3章 数据（Data）**。由于这一章内容非常详尽，我将分段翻译，先完成**3.1 数据收集（Data Collection）**与**3.2 数据过滤（Data Filtering）**的完整翻译。

---

### **第3章 数据（Data）**

#### **3.1 数据收集（Data Collection）**

我们系统性地收集并标注了**数十亿图像-文本对**，以支持图像生成模型的训练。与单纯追求原始数据规模不同，我们**优先保证数据质量与分布均衡**，致力于构建一个**贴近真实世界场景、类别均衡、代表性强的数据集**。

如图9所示，整个数据集被划分为**四大主类**：

| 类别 | 占比 | 子类与说明 |
|---|---|---|
| **Nature（自然）** | ~55% | 包含物体、风景、城市、植物、动物、室内、食物等子类。无法明确归类于“人物”或“设计”的内容也归入此类，是提升模型生成真实自然图像的基础。 |
| **Design（设计）** | ~27% | 海报、UI界面、PPT幻灯片、绘画、雕塑、手工艺品、数字艺术等。这类数据富含文本、复杂布局与设计语义，对提升复杂提示遵循与文本渲染能力至关重要。 |
| **People（人物）** | ~13% | 人像、运动、日常活动等。该类别帮助模型生成真实、多样的人物图像，提升用户体验与实际可用性。 |
| **Synthetic（合成数据）** | ~5% | 仅通过**受控文本渲染技术**合成的文本图像，**不包括其他AI模型生成的图像**。我们保守地避免使用外部AI合成数据，以防止视觉伪影、文本失真、偏见与幻觉等问题。 |

---

#### **3.2 数据过滤（Data Filtering）**

为在迭代开发过程中持续精选高质量训练数据，我们提出**七阶段流水线过滤策略**（图10）。这些阶段**顺序执行**，并在训练过程中**动态调整数据分布**。合成数据从**第4阶段**开始引入，此时基础模型已具备一定稳定性。

---

##### **阶段1：初始预训练数据整理（Initial Pre-training Data Curation）**

- **分辨率**：统一缩放至256×256像素，支持多种宽高比（1:1, 2:3, 3:2, 3:4, 4:3, 9:16, 16:9, 1:3, 3:1）。
- **过滤规则**：
  - **损坏文件过滤**：剔除损坏或截断图像（通常文件大小异常）。
  - **文件大小过滤**：移除过小文件。
  - **分辨率过滤**：原始分辨率低于256p的图像剔除。
  - **去重过滤**：移除重复或近似重复的图像-文本对。
  - **NSFW过滤**：排除包含性、暴力或其他冒犯性内容的图像。

---

##### **阶段2：图像质量增强（Image Quality Enhancement）**

- **旋转过滤**：移除EXIF元数据显示明显旋转或翻转的图像。
- **清晰度过滤**：剔除模糊或失焦图像。
- **亮度过滤（Luma Filter）**：排除过曝或过暗图像。
- **饱和度过滤**：移除色彩过度饱和（可能为人工渲染或数字篡改）的图像。
- **熵过滤**：剔除低熵图像（大面积纯色或内容极少）。
- **纹理过滤**：移除纹理过度复杂（可能为噪声或非语义模式）的图像。

---

##### **阶段3：图文对齐优化（Image-Text Alignment Improvement）**

- **数据划分**：
  - **原始字幕组（Raw Caption Split）**：网站原始字幕、标题或标签。虽含噪声，但提供真实世界知识（如植物名、卡通IP）。
  - **重字幕组（Recaption Split）**：使用最先进的Qwen-VL Captioner生成的结构化字幕。
  - **融合字幕组（Fused Caption Split）**：原始与合成字幕结合，兼顾通用性与细节。
- **过滤规则**：
  - **中文CLIP过滤**与**SigLIP 2过滤**：移除图文不匹配对。
  - **字幕长度过滤**：剔除过长字幕。
  - **无效字幕过滤**：移除异常内容（如“对不起，我无法为该图像提供字幕”）。

---

##### **阶段4：文本渲染增强（Text Rendering Enhancement）**

- **按语言划分数据集**：
  - 英语组（English Split）
  - 中文组（Chinese Split）
  - 其他语言组（Other Language Split）
  - 无文本组（Non-Text Split）
- **合成数据引入**：针对低频字符、混合语言、字体多样性等问题，使用**受控文本渲染技术**合成数据（详见3.4节）。
- **过滤规则**：
  - **密集文本过滤**：移除文本过于密集的图像。
  - **小字符过滤**：移除字符过小的图像（难以准确标注与渲染）。

---

##### **阶段5：高分辨率精修（High-Resolution Refinement）**

- **分辨率升级**：训练图像分辨率提升至640×640像素。
- **过滤规则**：
  - **图像质量过滤**：移除过曝、欠曝、模糊、压缩伪影等缺陷图像。
  - **分辨率过滤**：确保所有图像满足最低分辨率要求。
  - **美学过滤**：移除构图或视觉吸引力差的图像。
  - **异常元素过滤**：移除含水印、二维码、条形码等干扰元素的图像。

---

##### **阶段6：类别平衡与人像增强（Category Balance and Portrait Augmentation）**

- **重分类**：将数据集划分为三大类别：
  - 通用（General）
  - 人像（Portrait）
  - 文本渲染（Text Rendering）
- **数据增强**：
  - 使用关键词与图像检索技术，针对性补充**欠代表类别**。
  - 检索高质量人像（真实人像、卡通角色、名人），生成强调面部特征、表情、服饰、背景、光照、情绪等细节的字幕。
- **额外过滤**：移除含马赛克或模糊人脸的图像（避免隐私问题并提升鲁棒性）。

---

##### **阶段7：均衡多尺度训练（Balanced Multi-Scale Training）**

- **多分辨率训练**：联合训练640×640与1328×1328像素图像。
- **严格分辨率阈值问题**：若强制1328p分辨率，会损失大量数据并扭曲分布。
- **解决方案**：
  - 设计**层级分类系统**（受WordNet启发），按类别保留最高质量、最具美学价值的图像。
  - 采用**专门的重采样策略**，平衡文本渲染数据的长尾分布（低频字符问题）。
  - 通过多尺度训练，模型在适应高分辨率输入的同时，保留先前学习的通用知识，确保稳定收敛，提升细节生成能力且不牺牲泛化性。

---



我们继续翻译 **3.3 数据标注（Data Annotation）** 与 **3.4 数据合成（Data Synthesis）** 的完整内容。

---

### **3.3 数据标注（Data Annotation）**

在我们的数据标注流水线中，我们使用一个强大的图像字幕生成器（如 Qwen2.5-VL），不仅生成全面的图像描述，还同时生成**结构化元数据**，用于捕获图像的关键属性与质量信息。

与传统方法将字幕生成与元数据提取视为两个独立任务不同，我们设计了一个统一的标注框架，使得字幕生成器**在一次前向过程中**即可完成以下任务：

- **生成详细图像描述**（自然语言形式）
- **提取结构化元数据**（JSON格式）

#### **标注框架细节**

- **字幕生成要求**：
  1. 使用自然、描述性语言，不使用结构化格式或富文本。
  2. 丰富细节：包括物体属性、物体间视觉关系、环境背景。
  3. **识别并引用图像中可见的文本**，用引号标注，**不进行翻译或解释**。
  4. 保持真实性与准确性，避免泛泛而谈。

- **结构化元数据提取**：
  1. **图像类型识别**：根据来源与用途返回图像类型。
  2. **图像风格识别**：根据整体视觉特征返回风格描述。
  3. **水印检测**：以列表形式返回检测到的水印。
  4. **异常元素检测**：检查是否存在影响观看的元素（如二维码、马赛克等），返回“是/否”。

#### **示例输出格式（JSON）**

```json
{
  "Caption": "A vintage wooden table supports an antique typewriter...",
  "Image Type": "Real-world photograph",
  "Image Style": "Vintage, dramatic lighting",
  "Watermark List": ["© Example Studio"],
  "Abnormal Element": "no"
}
```

#### **效率与可扩展性**

- 借助先进字幕生成器，**无需额外模型或后处理步骤**，即可高效处理大规模数据集。
- 通过集成专家规则与轻量级分类模型，进一步优化关键任务（如水印验证、内容审核）。

---

### **3.4 数据合成（Data Synthesis）**

为应对真实世界图像中文本内容的长尾分布问题（尤其是中文等非拉丁语言中大量极低频字符），我们提出**多阶段文本感知图像合成流水线**。该流水线整合**三种互补策略**：

- **纯渲染（Pure Rendering）**
- **组合渲染（Compositional Rendering）**
- **复杂渲染（Complex Rendering）**

#### **3.4.1 纯渲染（Pure Rendering in Simple Backgrounds）**

- **目标**：训练模型识别并生成字符（如中英文）。
- **方法**：
  - 从大规模高质量语料中提取文本段落。
  - 使用**动态布局算法**将文本渲染到纯色背景上，自动调整字体大小与间距。
  - **严格质量控制**：若段落中任一字符无法渲染（如字体缺失或渲染错误），则**整段丢弃**，确保字符级文本保真度。

#### **3.4.2 组合渲染（Compositional Rendering in Contextual Scenes）**

- **目标**：将合成文本嵌入真实视觉上下文，模拟日常场景。
- **方法**：
  - 将文本模拟为书写或印刷在各种物理介质（如纸张、木板）上。
  - 无缝合成到多样化背景图像中，形成视觉一致的完整场景。
  - 使用 **Qwen-VL Captioner** 为每张合成图像生成描述性字幕，捕捉文本与环境的上下文关系。
- **效果**：显著提升模型在真实场景中理解与生成文本的能力。

#### **3.4.3 复杂渲染（Complex Rendering in Structured Templates）**

- **目标**：增强模型对复杂、结构化提示的遵循能力，尤其是涉及布局敏感内容。
- **方法**：
  - 使用**预定义模板**（如PPT幻灯片、UI设计稿）进行程序化编辑。
  - 设计**基于规则的系统**，自动替换占位符文本，同时保持布局结构、对齐与格式完整。
- **意义**：帮助模型理解并执行涉及多行文本、精确空间布局、字体控制等复杂指令。

#### **整体意义**

我们的数据合成框架系统性地解决了自然图像数据集中文本内容稀缺与不平衡的挑战。通过整合**简洁性、真实性与结构复杂性**的多重渲染策略，合成出全面且多样的训练数据，从而增强模型在各类文本渲染任务中的鲁棒性，最终提升其根据复杂用户提示生成高质量图像的能力。

---

好的，我们继续翻译**第4章 训练（Training）**。由于内容较长，我将分节翻译，先完成**4.1 预训练（Pre-training）**的全部内容，包括训练目标、Producer-Consumer框架、分布式训练优化与训练策略。

---

### **第4章 训练（Training）**

#### **4.1 预训练（Pre-training）**

我们采用**流匹配（flow matching）训练目标**对 Qwen-Image 进行预训练。该方法通过常微分方程（ODE）实现稳定的学习动态，同时保持与最大似然目标的等价性。

##### **数学形式化**

- 设输入图像的潜变量为 \( x_0 \)，通过 VAE 编码器得到潜表征 \( z = E(x_0) \)。
- 从标准多元正态分布中采样噪声向量 \( x_1 \sim \mathcal{N}(0, I) \)。
- 根据**修正流（Rectified Flow）**，在扩散时间步 \( t \in [0, 1] \) 时，中间潜变量及其速度定义为：

  \[
  \begin{cases}
  x_t = t x_0 + (1 - t) x_1 \\
  v_t = \frac{dx_t}{dt} = x_0 - x_1
  \end{cases}
  \]

- 模型训练目标为预测速度 \( v_t \)，损失函数为预测值与真值之间的均方误差（MSE）：

  \[
  \mathcal{L} = \mathbb{E}_{(x_0, h) \sim \mathcal{D}, x_1, t \sim \mathcal{U}(0,1)} \| v_\theta(x_t, t, h) - v_t \|^2
  \]

其中 \( v_\theta \) 为模型预测速度，\( \mathcal{D} \) 为训练数据集。

---

#### **4.1.1 Producer-Consumer 框架（Producer–Consumer Framework）**

为在大规模 GPU 集群上同时实现**高吞吐**与**训练稳定性**，我们采用受 Ray 启发的 **Producer-Consumer 框架**，将**数据预处理**与**模型训练**解耦：

- **Producer 端**：
  - 对原始图像-字幕对进行过滤（分辨率、检测算子等）。
  - 使用 MLLM（如 Qwen2.5-VL）与 VAE 编码为潜变量。
  - 按分辨率分组缓存至高速缓存桶（cache buckets），存入共享存储。
  - 通过 HTTP 传输层与 Consumer 端异步通信，支持零拷贝调度。

- **Consumer 端**：
  - 部署于 GPU 密集型集群，**仅专注模型训练**。
  - 通过 4-way 张量并行（tensor parallel）分布 MMDiT 参数。
  - 每个数据并行组异步从 Producer 拉取预处理批次。

---

#### **4.1.2 分布式训练优化（Distributed Training Optimization）**

由于 Qwen-Image 参数量巨大，仅使用 FSDP（Fully Sharded Data Parallel）不足以在单 GPU 上容纳模型，因此我们基于 Megatron-LM 进行以下优化：

##### **混合并行策略（Hybrid Parallelism Strategy）**

- **数据并行 + 张量并行**：
  - 使用 Transformer-Engine 库构建 MMDiT，支持自动张量并行度切换。
  - 对多头自注意力模块，采用**按头并行（head-wise parallelism）**，减少通信开销。

##### **分布式优化器与激活检查点（Activation Checkpointing）**

- 为缓解 GPU 内存压力，我们对比了**激活检查点**与**分布式优化器**：
  - 启用激活检查点：GPU 内存从 71GB 降至 63GB（-11.3%），但每轮训练时间从 2s 增至 7.5s（+3.75×）。
- **最终选择**：**禁用激活检查点**，仅使用**分布式优化器**。
- **数值精度**：
  - 前向与梯度通信使用 **bfloat16**。
  - 梯度规约（reduce-scatter）使用 **float32**，兼顾效率与数值稳定性。

---

#### **4.1.3 训练策略（Training Strategy）**

我们采用**多阶段渐进预训练策略**，逐步提升数据质量、图像分辨率与模型性能：

| 策略维度 | 具体实施 |
|---|---|
| **分辨率提升** | 从 **256×256**（多宽高比）→ **640×640** → **1328×1328**。 |
| **文本渲染引入** | 从**无文本图像**起步，逐步引入含文本图像，解决中文等语言字符稀缺问题。 |
| **数据质量精炼** | 初期使用大规模数据，后期逐步采用更严格过滤机制，仅保留最高质量样本。 |
| **数据分布平衡** | 逐步平衡领域与分辨率分布，防止过拟合特定领域或分辨率。 |
| **合成数据增强** | 针对**超现实风格**或**高分辨率文本丰富图像**等稀缺分布，使用**数据合成技术**补充样本。 |

---

我们继续翻译**4.2 后训练（Post-training）**的完整内容，包括**4.2.1 监督微调（SFT）**与**4.2.2 强化学习（RL）**。

---

### **4.2 后训练（Post-training）**

后训练框架分为两个阶段：

- **监督微调（SFT）**：针对模型缺陷构建高质量、细粒度标注数据。
- **强化学习（RL）**：使用 **DPO（Direct Preference Optimization）** 与 **GRPO（Group Relative Policy Optimization）** 进一步优化。

---

#### **4.2.1 监督微调（Supervised Fine-Tuning, SFT）**

- **数据构建**：
  - 按**语义类别层级**组织数据集。
  - **人工精细标注**，确保图像**清晰、细节丰富、明亮且真实**。
- **目标**：
  - 引导模型生成**更具真实感与细节质量**的内容。
  - 针对预训练阶段暴露出的**特定缺陷**（如复杂文本渲染、细节一致性）进行定向改进。

---

#### **4.2.2 强化学习（Reinforcement Learning, RL）**

我们采用两种互补的 RL 策略：

| 方法 | 特点 | 用途 |
|---|---|---|
| **DPO（Direct Preference Optimization）** | 离线偏好建模，计算高效 | 大规模 RL 训练 |
| **GRPO（Group Relative Policy Optimization）** | 在线采样，轨迹级奖励评估 | 小规模精细调优 |

---

##### **(A) Direct Preference Optimization（DPO）**

- **数据准备**：
  - 对同一提示，使用不同随机种子生成多张图像。
  - **人工标注者**从中选出**最佳**与**最差**图像。
  - 数据分为两类：
    - **有参考图像**：与真实参考对比，差异显著时标记为“拒绝样本”。
    - **无参考图像**：在生成结果中选出最佳与最差，或标记为“全部不满意”。

- **算法**：
  - 给定文本表征 \( h \)、**优选图像** \( x_{\text{win}}^0 \) 与**拒绝图像** \( x_{\text{lose}}^0 \)。
  - 从 \( t \sim \mathcal{U}(0,1) \) 采样，构造中间潜变量 \( x_t \) 与速度 \( v_t \)。
  - 基于流匹配训练准则，构造 DPO 目标：

    \[
    \mathcal{L}_{\text{DPO}} = -\mathbb{E}_{h,(x_{\text{win}}^0,x_{\text{lose}}^0)\sim\mathcal{D},t\sim\mathcal{U}(0,1)} \log \sigma\!\left(-\beta\bigl(\text{Diff}_{\text{policy}} - \text{Diff}_{\text{ref}}\bigr)\right)
    \]

    其中：

    - \( \text{Diff}_{\text{policy}} \) 与 \( \text{Diff}_{\text{ref}} \) 分别为策略模型与参考模型的偏好差异。
    - \( \beta \) 为缩放系数，\( \sigma \) 为 Sigmoid 函数。

---

##### **(B) Group Relative Policy Optimization（GRPO）**

- **算法流程**：
  - 在 DPO 训练后，使用 GRPO 进行**更细粒度的微调**。
  - 给定文本表征 \( h \)，流模型生成一组 \( G \) 张图像 \( \{x_i^0\}_{i=1}^G \) 与其完整轨迹。
  - 对每组图像，计算优势函数：

    \[
    A_i = \frac{R(x_i^0, h) - \text{mean}\!\bigl(\{R(x_i^0, h)\}_{i=1}^G\bigr)}{\text{std}\!\bigl(\{R(x_i^0, h)\}_{i=1}^G\bigr)}
    \]

  - GRPO 训练目标：

    \[
    \mathcal{L}_{\text{GRPO}}(\theta) = \mathbb{E}_{h\sim\mathcal{D},\{x_i^T,\dots,x_i^0\}\sim\pi_\theta} \frac{1}{G}\sum_{i=1}^G \frac{1}{T}\sum_{t=0}^{T-1} \min\!\bigl(r_t^i(\theta)A_i,\text{clip}(r_t^i(\theta),1-\varepsilon,1+\varepsilon)A_i\bigr) - \beta D_{\text{KL}}(\pi_\theta\|\pi_{\text{ref}})
    \]

    其中 \( r_t^i(\theta) = \frac{p_\theta(x_i^{t-1}|x_i^t,h)}{p_{\theta_{\text{old}}}(x_i^{t-1}|x_i^t,h)} \)。

- **随机采样增强**：
  - 流匹配采样本身无随机性，不利于探索。
  - 将采样过程改写为**随机微分方程（SDE）**：

    \[
    dx_t = \Bigl(v_\theta(x_t,t,h) + \tfrac{\sigma_t^2}{2t}\bigl(x_t + (1-t)v_\theta(x_t,t,h)\bigr)\Bigr)dt + \sigma_t\sqrt{dt}\,\varepsilon
    \]

  - 通过 Euler–Maruyama 离散化实现：

    \[
    x_{t+\Delta t} = x_t + \Bigl[v_\theta(x_t,t,h) + \tfrac{\sigma_t^2}{2t}\bigl(x_t + (1-t)v_\theta(x_t,t,h)\bigr)\Bigr]\Delta t + \sigma_t\sqrt{\Delta t}\,\varepsilon
    \]

---

### **4.3 多任务训练（Multi-task Training）**

除文本到图像（T2I）外，我们扩展基础模型以支持**多模态图像生成任务**，包括：

- **指令式图像编辑（instruction-based image editing）**
- **新视角合成（novel view synthesis）**
- **计算机视觉任务**：深度估计、边缘检测、超分辨率等

这些任务可统一视为**通用图像编辑任务（TI2I）**。

#### **输入处理**

- **文本+图像输入**：
  - 用户图像通过 **Vision Transformer (ViT)** 编码为视觉 patch。
  - 与文本 token 拼接后输入 Qwen-Image MMDiT 的文本流。
  - 系统提示如图15所示。

- **VAE 潜变量输入**：
  - 将输入图像的 VAE 潜变量拼接至带噪图像潜变量（沿序列维度）。
  - 借鉴先前工作，**保持身份与场景一致性**。

- **多图像区分**：
  - 扩展 MSRoPE，增加**帧维度（frame dimension）**，区分编辑前/后图像（图14右）。

#### **实验发现**

- **MLLM 语义嵌入**：提升指令遵循能力。
- **VAE 像素级嵌入**：进一步增强视觉保真度与结构一致性。


我们进入第5章《实验（Experiments）》的完整翻译。由于内容较长，先给出 5.1 与 5.2 的译文，下一回复再给出 5.3。

---

### **第5章 实验（Experiments）**

#### **5.1 人工评估（Human Evaluation）—— AI Arena**

为全面评估 Qwen-Image 的通用图像生成能力，并与最先进的闭源 API 进行客观比较，我们搭建了 **AI Arena**（https://aiarena.alibaba-inc.com）——一个基于 Elo 评分系统的开放基准平台（图 16）。

**AI Arena 的核心机制**  
- 每一轮，平台随机选取两个匿名模型，根据同一提示生成两张图像，由用户进行两两对比投票。  
- 用户可选“左图更好 / 右图更好 / 两者都好 / 两者都差”。  
- 利用 Elo 算法实时更新个人与全局排行榜。  
- 已开放给公众，未来将扩展到文本→音频、文本→视频等多模态任务。

**实验设置**  
- **提示集**：5 000 条多样化提示，涵盖主题、风格、摄影视角等维度。  
- **评估者**：200+ 来自不同专业背景的评估者。  
- **对照模型**（闭源 API）  
  - Imagen 4 Ultra Preview 0606  
  - Seedream 3.0  
  - GPT Image 1 [High]  
  - FLUX.1 Kontext [Pro]  
  - Ideogram 3.0  

**结果**（图 16）  
- Qwen-Image 在 AI Arena 中排名第 3（唯一上榜的开源模型）。  
- 与第 1 名 Imagen 4 相差 30 Elo，但领先 GPT Image 1 与 FLUX.1 Kontext [Pro] 30+ Elo。  
- 说明 Qwen-Image 在开放生态中具有强大竞争力。

---

#### **5.2 定量结果（Quantitative Results）**

##### **5.2.1 VAE 重建性能**

| 模型 | Enc 参 | Dec 参 | ImageNet-256 | Text-256 |
|---|---|---|---|---|
| Wan2.1-VAE | 19 M | 25 M | 31.29 / 0.887 | 26.77 / 0.939 |
| Hunyuan-VAE | 34 M | 50 M | 33.21 / 0.914 | 32.83 / 0.977 |
| FLUX-VAE | 34 M | 50 M | 32.84 / 0.916 | 32.65 / 0.979 |
| **Qwen-Image-VAE** | 19 M | 25 M | **33.42 / 0.916** | **36.63 / 0.984** |

- **结论**：Qwen-Image-VAE 在所有指标上均达到 SOTA，尤其在文本丰富图像上显著领先。

---

##### **5.2.2 文本到图像生成**

###### **(1) DPG 基准（1 k 密集提示）**

| 模型 | Global | Entity | Attribute | Relation | Other | Overall↑ |
|---|---|---|---|---|---|---|
| SDXL | 83.27 | 82.43 | 80.91 | 86.76 | 80.41 | 74.65 |
| FLUX.1 [Dev] | 74.35 | 90.00 | 88.96 | 90.87 | 88.33 | 83.84 |
| GPT Image 1 | 88.89 | 88.94 | 89.84 | 92.63 | 90.96 | 85.15 |
| **Qwen-Image** | **91.32** | **91.56** | **92.02** | **94.31** | **92.73** | **88.32** |

- Qwen-Image 全局第一，尤其擅长属性与关系理解。

###### **(2) GenEval 基准（组合提示）**

| 模型 | Single | Two | Count | Colors | Position | Attr | Overall↑ |
|---|---|---|---|---|---|---|---|
| GPT Image 1 | 0.99 | 0.92 | 0.85 | 0.92 | 0.75 | 0.61 | 0.84 |
| Seedream 3.0 | 0.99 | 0.96 | 0.91 | 0.93 | 0.47 | 0.80 | 0.84 |
| **Qwen-Image-RL** | **1.00** | **0.95** | **0.93** | **0.92** | **0.87** | **0.83** | **0.91** |

- Qwen-Image-RL **唯一超过 0.9**，展现卓越可控生成能力。

###### **(3) OneIG-Bench（中英双语）**

| 模型 | 英文 Overall↑ | 中文 Overall↑ |
|---|---|---|
| GPT Image 1 | 0.533 | 0.474 |
| Seedream 3.0 | 0.530 | 0.528 |
| **Qwen-Image** | **0.539** | **0.548** |

- 中英通道均排名第一，文本渲染尤其突出。

###### **(4) 文本渲染专项**

- **CVTG-2K（英文渲染）**  
  - Word Accuracy：Qwen-Image 0.828（Seedream 0.592，GPT 0.857）  
  - CLIPScore：0.802（与最佳持平）

- **ChineseWord（中文单字）**  
  | 模型 | Level-1 | Level-2 | Level-3 | Overall↑ |
  |---|---|---|---|---|
  | Seedream 3.0 | 53.48 | 26.23 | 1.25 | 33.05 |
  | GPT Image 1 | 68.37 | 15.97 | 3.55 | 36.14 |
  | **Qwen-Image** | **97.29** | **40.53** | **6.48** | **58.30** |

- **LongText-Bench（长文本）**  
  | 模型 | 英文↑ | 中文↑ |
  |---|---|---|
  | GPT Image 1 | 0.956 | 0.619 |
  | Seedream 3.0 | 0.896 | 0.878 |
  | **Qwen-Image** | **0.943** | **0.946** |

---

##### **5.2.3 图像编辑（TI2I）**

###### **(1) GEdit-Bench（真实用户指令）**

| 模型 | GEdit-Bench-EN↑ | GEdit-Bench-CN↑ |
|---|---|---|
| GPT Image 1 [High] | 7.53 | 7.30 |
| **Qwen-Image** | **7.56** | **7.52** |

- 中英双榜第一。

###### **(2) ImgEdit-Bench（9 类编辑任务）**

| 模型 | Overall↑ |
|---|---|
| GPT Image 1 [High] | 4.20 |
| **Qwen-Image** | **4.27** |

- 综合第一，指令遵循与细节保持最佳。

###### **(3) 新视角合成（GSO 数据集）**

| 模型 | PSNR↑ | SSIM↑ | LPIPS↓ |
|---|---|---|---|
| Zero-123 | 13.48 | 0.854 | 0.166 |
| CRM | 15.93 | 0.891 | 0.152 |
| **Qwen-Image** | **15.11** | **0.884** | **0.153** |

- 通用模型中最佳，逼近专用 3D 模型。

###### **(4) 深度估计（Zero-shot）**

| 数据集 | Metric | MiDaS | DPT | **Qwen-Image** |
|---|---|---|---|---|
| KITTI | AbsRel↓ | 0.236 | 0.100 | **0.078** |
| | δ1↑ | 0.630 | 0.901 | **0.951** |
| NYUv2 | AbsRel↓ | 0.111 | 0.098 | **0.055** |
| | δ1↑ | 0.885 | 0.903 | **0.967** |

- 在 5 个零样本数据集上，Qwen-Image 与专用深度模型持平或更优。

---

### **小结**

- **人工评估**：AI Arena 排名第 3，领先多数闭源 API。
- **基准测试**：在文本渲染、图像编辑、3D 深度/新视角任务中均取得 **SOTA 或接近 SOTA**。
- **中文长文本**：显著领先现有模型，验证课程式训练与合成数据有效性。

---

好的，我将继续翻译**5.3 定性结果（Qualitative Results）**。由于内容较多，我会分段翻译，先完成**VAE 重建可视化**与**文本渲染**部分。

---

### **5.3 定性结果（Qualitative Results）**

#### **5.3.1 VAE 重建可视化（VAE Reconstruction）**

图17展示了不同 VAE 模型对文本丰富图像的重建结果。第一行展示了包含英文文本的 PDF 图像的重建效果。在我们的结果中，“double-aspect”这一短语清晰可读，而其他模型的重建结果中该短语则难以辨认。总体而言，Qwen-Image-VAE 在小文本重建方面表现更为精准。

#### **5.3.2 文本渲染（Text Rendering）**

##### **(1) 英文文本渲染（English Text Rendering）**

图18和图19展示了英文文本渲染的定性比较结果。如图18所示，Qwen-Image 在渲染长英文段落时表现出更接近现实的视觉风格和更好的渲染质量。与之相比，GPT Image 1 [High] 和 Recraft V3 在文本渲染中存在错误（如“lantern”和“Unfurling”错误，以及“silver”和“quiet”错误），而 Seedream 3.0 的文本则显得冗余且扭曲。在图19的上半部分，Qwen-Image 成功渲染了七个不同位置的文本，展现了其复杂的文本渲染能力。相比之下，GPT Image 1 漏掉了“The night circus”，而 Seedream 3.0 和 Hidream-I1-Full 的文本渲染结果则出现扭曲。在图19的下半部分，Qwen-Image 不仅成功渲染了每个文本段落，还呈现了一个布局合理且视觉上美观的幻灯片。相比之下，GPT Image 1 漏掉了“Stay Connected”，而 Hidream-I1-Full 和 Seedream 3.0 未能正确渲染文本。

##### **(2) 中文文本渲染（Chinese Text Rendering）**

图20和图21展示了中文文本渲染的定性比较结果。在图20中，Qwen-Image 准确生成了预期的对联，忠实地再现了文本的内容和风格，并准确描绘了所需的房间布局和位置。相比之下，GPT Image 1 和 Seedream 3.0 漏掉或生成了扭曲的字符（GPT Image 1 漏掉了“远”和“善”，Seedream 3.0 漏掉了“智”和“机”），而其他模型则无法生成正确的对联。在图21的上半部分，Qwen-Image 能够正确生成多个字符和牌匾，完美遵循输入提示的空间布局和文本渲染要求。Seedream 3.0 在复杂的空间布局中遇到困难，漏掉了一些场景和字符，而其他模型则无法正确理解复杂的文本和空间指令。图21的下半部分显示，Qwen-Image 能够在复杂场景中生成逼真且排版精美的手写文本，完美遵循输入提示，而其他模型则难以生成结构化的段落文本。

##### **(3) 多对象生成（Multi-Object Generation）**

图22展示了多对象生成的定性比较结果。在上半部分，Qwen-Image 准确生成了所有要求的动物，并忠实地保留了它们指定的位置，一致地应用了正确的毛绒风格。相比之下，GPT Image 1 未能生成毛绒风格的图像，而 Recraft V3 和 Seedream 3.0 生成的动物与提示不匹配。在图22的下半部分，Qwen-Image 不仅正确渲染了台球上的双语文本，还严格遵循了将台球排列成两行的指令。GPT Image 1 未能完全遵循布局要求，并且错误地生成了一个中文字符“發”，而其他模型则无法正确生成大多数中文文本。

##### **(4) 空间关系生成（Spatial Relationship Generation）**

图23展示了空间关系生成的定性比较结果。在第一部分中，Qwen-Image 生成的图像准确反映了提示，捕捉到了两个攀岩者之间的正确互动。相比之下，GPT Image 1、Seedream 3.0 和 Recraft V3 未能完全遵循提示：这些模型生成的攀岩者之间的互动是错误的。这一比较表明，Qwen-Image 在理解和精确遵循复杂提示方面表现出色。在剩余的两个部分中，只有 Qwen-Image 和 GPT Image 1 能够准确描绘人物与鸽子之间的空间关系，以及怀表与杯子之间的空间关系。

---

好的，我们继续翻译**5.3.3 图像编辑（Image Editing）**部分，涵盖以下内容：

- **文本与材质编辑（Text and Material Editing）**
- **对象增删改（Object Addition/Removal/Replacement）**
- **姿态调整（Pose Manipulation）**
- **链式编辑（Chained Editing）**
- **新视角合成（Novel View Synthesis）**

---

### **5.3.3 图像编辑（Image Editing）**

#### **(1) 文本与材质编辑（Text and Material Editing）**

图24展示了文本和材质编辑能力的定性比较结果。在顶部的例子中，涉及编辑具有复杂风格的文本，Seedream 3.0 未能将字母“H”更改为“Q”，而 GPT Image 1 [High] 未能保留原始风格。Qwen-Image 和 FLUX.1 Kontext [Pro] 成功地修改了文本，同时保持了风格一致性。在底部的例子中，除了 FLUX.1 Kontext [Pro] 外，所有模型都准确地添加了所需的文本和相关元素。值得注意的是，Qwen-Image 是唯一正确生成请求的珐琅彩色玻璃艺术品的模型，展示了卓越的材质渲染和指令遵循能力。

#### **(2) 对象增删改（Object Addition/Removal/Replacement）**

图25比较了各种模型在具有挑战性的现实场景中的表现。除了 GPT Image 1 [High] 经常无法保持整体图像一致性外，其他模型在保留未编辑区域方面表现良好。在顶部的例子中，任务是添加一只猫和一只狗，且风格为卡通，模型必须确保新对象与整体艺术风格一致。我们发现 FLUX.1 Kontext [Pro] 在编辑非写实图像时一致性较差，而 SeedEdit 3.0 和 Qwen-Image 产生了与期望卡通风格一致的连贯结果。在中间的例子中，涉及从拥挤场景中移除所有人——这是一个相对复杂的指令——所有模型都准确完成了任务，仅在细节上有所不同。此外，我们注意到在不同模型中偶尔会出现缩放效果。

#### **(3) 姿态调整（Pose Manipulation）**

图26展示了不同模型的姿态调整能力的定性比较结果。在第一个例子中，只有 FLUX.1 Kontext [Pro] 和 Qwen-Image 能够在姿态编辑过程中保留诸如人物发丝等细节。在第二个例子中，需要在姿态变化时保持服装一致性和场景稳定性，Qwen-Image 是唯一保持背景和人物不变的模型。值得注意的是，Qwen-Image 准确地从输入中推断出人物穿着的是带有侧开叉的裙子，内搭丝质长裤，并在站立姿态中忠实地展现出丝质长裤。在第三个例子中，Qwen-Image 再次超越其他模型，更好地保留了原始姿态并保持了服装的一致性。

#### **(4) 链式编辑（Chained Editing）**

链式编辑指的是生成的图像被迭代地用作后续编辑步骤的上下文。图27的第一个例子中，任务要求提取人物的外套产品，并展示其面料细节的特写。我们选择了一幅中国风的传统绘画作为源图像。SeedEdit 3.0 和 FLUX.1 Kontext [Pro] 在第一个提示时就失败了，而 GPT Image 1 [High] 和 Qwen-Image 准确地提取了配对的鸟。Qwen-Image 在保留细节纹理方面优于 GPT Image 1 [High]。在第二个例子中，输入图像是一艘双开尾的船。Qwen-Image 和 FLUX.1 Kontext [Pro] 都能够在整个链式编辑过程中保持这一结构特征。然而，FLUX.1 Kontext [Pro] 未能按照指令添加两艘货船，而 Qwen-Image 成功完成了完整的编辑。

#### **(5) 新视角合成（Novel View Synthesis）**

图28评估了不同模型的空间推理和新视角合成能力。SeedEdit 3.0 和 FLUX.1 Kontext [Pro] 在相同的指令下无法很好地执行视角旋转。虽然 GPT Image 1 [High] 在有明确主体时可以生成新的视角，但它无法推广到具有复杂多物体的现实场景。只有 Qwen-Image 在全局一致性方面表现出色，包括文本保真度和光照结构，展示了在复杂编辑中卓越的空间和语义连贯性。

---

好的，我们继续翻译**第6章 结论（Conclusion）**。

---

### **6 结论（Conclusion）**

在本文中，我们介绍了 Qwen-Image，这是 Qwen 系列中的一款图像生成基础模型，在复杂文本渲染和精准图像编辑方面取得了重大进展。通过构建全面的数据流水线并采用渐进式课程学习策略，Qwen-Image 显著提升了其在生成图像中渲染复杂文本的能力。此外，我们提出的改进的多任务训练范式和双重编码机制显著增强了图像编辑的一致性和质量，有效提升了语义连贯性和视觉保真度。在多个公开基准上的广泛实验一致表明，Qwen-Image 在广泛的图像生成和编辑任务中均达到了**当前最佳性能**。这些结果不仅凸显了 Qwen-Image 的技术稳健性，还证明了其在现实世界多模态场景中的广泛适用性，标志着大型基础模型发展的一个重要里程碑。

我们现在进一步探讨 Qwen-Image 的更广泛影响和意义：

**Qwen-Image 作为“图像生成”模型**：在图像生成的语境中，Qwen-Image 重新定义了生成建模的优先级。与其仅仅优化照片真实感或审美质量（“AI 风格”），Qwen-Image 更加注重文本与图像之间的精准对齐——尤其是在极具挑战性的文本渲染任务中。我们设想，通过在基础模型中强化这一能力，未来的界面可以从纯粹基于语言的 LUI（语言用户界面）进化为视觉-语言 VLU（视觉-语言用户界面）。当 LLM 在传达诸如颜色、空间关系或结构布局等视觉属性时遇到困难时，由 Qwen-Image 赋能的 VLU 可以生成丰富且图文并茂的图像——实现结构化的视觉解释和有效的知识外化，将复杂的想法转化为易于理解的多模态形式。

**Qwen-Image 作为“图像理解”模型**：在图像理解的语境中，Qwen-Image 展示了生成框架可以有效地执行经典理解任务。例如，在深度估计方面，尽管 Qwen-Image 并未超越专门的判别模型，但其性能与之非常接近。至关重要的是，与传统专家模型依赖于判别式理解（直接将输入映射到输出，而不建模底层分布）不同，Qwen-Image 利用了生成式理解：它首先构建了一个关于视觉内容的整体分布，深度自然地从中推导出来。这种从直接推理到分布推理的转变，为统一多模态系统开辟了新的途径。

**Qwen-Image 作为“3D 和视频生成”的图像模型**：在 3D 和视频生成的语境中，Qwen-Image 展示了其在 2D 图像合成之外的强大泛化能力。通过图像编辑的视角，我们将 Qwen-Image 应用于新视角合成，发现作为通用图像基础模型，它在几个具有挑战性的渲染场景中超越了专门的 3D 模型，展现出跨视角的卓越一致性。在姿态编辑任务中，尽管存在显著的运动变化，Qwen-Image 仍然在主体身份和背景结构方面保持了显著的连贯性——这是视频生成的一个基本要求。此外，与大多数依赖于图像 VAE 的图像生成方法不同，我们采用了视频 VAE 进行视觉表示。尽管这增加了建模的复杂性，但与我们的核心目标一致：构建一个能够泛化到多样化视觉模态的基础模型，而不仅仅是静态图像。

**Qwen-Image 作为“视觉生成”模型**：在集成理解与生成的语境中，Qwen-Image 推动了感知与创造无缝整合的愿景。我们主张，实现真正的理解-生成统一性依赖于三个基础支柱：（1）掌握理解；（2）掌握生成；（3）掌握它们的协同整合。作为 Qwen 系列中首个专注于视觉生成的工作，Qwen-Image 填补了第二个支柱——生成能力的关键空白，与在视觉理解（第一个支柱）方面表现出色的 Qwen2.5-VL 相辅相成。它们共同为下一代多模态 AI 奠定了平衡的基础，为 Visual-Language Omni 系统铺平了道路，这些系统不仅能够感知和推理，还能够生成文本丰富、视觉连贯的图像——将语言和视觉无缝融合成富有表现力、可读且语义忠实的视觉内容。

总之，Qwen-Image 不仅仅是一个当前最佳的图像生成模型，它代表了我们如何构思和构建多模态基础模型的范式转变。其贡献超越了技术基准，挑战社区重新思考生成模型在感知、界面设计和认知建模中的角色。通过在图像生成中强调复杂文本渲染，并通过图像编辑的视角解决经典理解任务（如深度估计），Qwen-Image 指向了一个未来：（1）生成模型不仅仅是生成图像，而是真正理解它们；（2）理解模型超越被动判别，通过内在的生成过程实现理解。随着我们继续扩展和精细化这样的系统，视觉理解和生成之间的界限将进一步模糊，为真正交互式、直观且智能的多模态系统铺平道路。

---

